[build]
builder = "nixpacks"
builderConfig = { nixpacksVersion = "1" }

[nixpacks]
nodeVersion = "20"

[deploy]
startCommand = "npm install && node server.js"
healthCheckPath = "/health"
healthCheckTimeout = 10

[environment]
PORT = "8080"
NODE_ENV = "production"
